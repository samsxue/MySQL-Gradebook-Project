# Changelog

## Unreleased

### Repository organization

- Moved the existing schema into `database/schema`.
- Moved the existing functions into `database/functions`.
- Added numeric filename prefixes to make the recommended order visible.
- Added project, contribution, and security documentation.
- Added common repository metadata files.

No SQL statements, database objects, parameters, return messages, sample calls, or
runtime behavior were changed in this cleanup.
